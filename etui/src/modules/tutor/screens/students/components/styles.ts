import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  container: {
    height: 30,
    width: '100%',
    alignItems: 'center',
  },
});
